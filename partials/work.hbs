{{#if resume.work.length}}
  <div id='work-block' class='section-block'>
    <div class='section-intro'>
      <div class='section-name'>
        <span>EXPERIENCE</span>
      </div>
      <div class='section-intro-line'></div>
    </div>
    <div class='section-content'>
      {{#each resume.work}}
        <div class='job-block'>
          <div class='block-header'>
            <span class='title' style="width: 63px; text-align: start; display: inline-block;">{{position}}</span>
            <span class='organization'>{{name}}</span>
            <span class='date'>
              <span style="text-align: end;"><time datetime="{{startDate}}">{{MONTH_YEAR startDate}}</time></span>
              <span>&mdash;</span>
              {{#if endDate}}<span style="width: 63px; text-align: start; display: inline-block;" ><time datetime="{{endDate}}">{{MONTH_YEAR endDate}}</time></span>
              {{else}}<span style="width: 63px; text-align: start; display: inline-block;" >Present</span>{{/if}}
            </span>
          </div>

          <div class='display-none'>
            {{!-- note: this is to enable any automated parsing of content --}}
            {{#if location}}
              <span class='location'>{{location}}</span>
            {{/if}}
            {{#if website}}
              <span class='website'><a href='{{website}}'>{{website}}</a></span>
            {{/if}}
          </div>

          {{#if highlights.length}}
            <ul class='highlights-work'>
              {{#each highlights}}
                <li>{{.}}</li>
              {{/each}}
            </ul>
          {{/if}}
          {{#unless @last}}
            <div class='separator'></div>
          {{/unless}}
        </div>
      {{/each}}
    </div>
  </div>
  <div class='section-line'></div>
{{/if}}
